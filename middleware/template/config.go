package template

// Config contains the configuration for the middleware
type Config struct{}

// DefaultConfig contains the default value for the
// middleware configuration
var DefaultConfig = &Config{}
